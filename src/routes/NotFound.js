import React from 'react';

export default ({}) => (
  <div style={{
    	height: window.innerHeight,
      backgroundRepeat: 'no-repeat',
    	backgroundImage: `url(${require('assets/images/404.png')})`
    }}>
  </div>
);
